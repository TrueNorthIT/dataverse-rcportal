/**
 * Delivery project (`msdyn_project`). Record shape from the generated schema
 * types (`dataverse.generated.ts`).
 */
export type { Project } from './dataverse.generated'

/** A project plan item — a phase bar or a milestone (`new_projecttask`). */
export type { Projecttask as ProjectTask } from './dataverse.generated'
