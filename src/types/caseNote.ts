/**
 * A note/update on a support case (`annotation`), exposed by the `casenotes`
 * route. Record shape from the generated schema types (`dataverse.generated.ts`),
 * aliased to `CaseNote`.
 */
export type { Casenotes as CaseNote } from './dataverse.generated'
